[workspace]
resolver = "3"
members = [
    "crates/htl-core",
    "crates/htl-macros",
    "crates/htl",
    "crates/htl-cli",
    "examples/embed",
    "examples/resolver",
]

[workspace.package]
version = "0.1.5"
edition = "2024"
authors = ["Yutaka Nishimura"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/ynishi/htl"
rust-version = "1.88"

[workspace.dependencies]
# default-features off at the workspace level so members opt in (`features = [...]`).
htl-core = { path = "crates/htl-core", version = "0.1.5", default-features = false }
htl-macros = { path = "crates/htl-macros", version = "0.1.5" }
htl = { path = "crates/htl", version = "0.1.5", default-features = false }
mlua = { version = "0.12.1", features = ["lua54", "vendored", "error-send"] }
mlua-pkg = "0.9.0"
anyhow = "1.0.104"
clap = { version = "4.6.6", features = ["derive"] }
walkdir = "2.5.0"
